/**
 * Application layer ports (interfaces)
 * These define contracts that the infrastructure layer must implement
 */

export type { IImageLoader, ImageData } from "./IImageLoader";
export type { IPhotoRepository, PhotoDescription } from "./IPhotoRepository";
